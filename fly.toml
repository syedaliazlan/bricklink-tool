# fly.toml app configuration file generated for bricklink-tool on 2025-12-26T01:30:01Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'bricklink-tool'
primary_region = 'lhr'

[build]

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024

# Network configuration to ensure outbound connections use the dedicated IP
# Note: Fly.io uses the assigned IPv4 for outbound traffic when properly configured
# The dedicated IP (149.248.197.193) should be used for all outbound connections
